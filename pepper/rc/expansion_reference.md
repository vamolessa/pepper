These are the available expansions when evaluating commands.
An expansion syntax has the form: `@expansion-name(expansion-args)`
where `expansion-args` can be empty and its semantics varies for each expansion.

# expansions

## `arg`

