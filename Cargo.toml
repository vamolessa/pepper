[workspace]
members = [
    "pepper",
    "plugin-lsp",
    "plugin-remedybg",
    "mine",
]
default-members = ["mine"]
exclude = ["web"]

[profile.release]
debug = true
incremental = true
lto = "off"

[profile.dev]
debug = true
incremental = true
lto = "off"

